# Server
PORT=8080

# PayPlus Sandbox (fill with your sandbox values)
# Example: https://sandbox.payplus.co.il
PAYPLUS_ORIGIN=
# Example template: https://sandbox.payplus.co.il/.../start?order_id={ORDER_ID}&amount={AMOUNT}
PAYPLUS_REDIRECT_TEMPLATE=
# HMAC secret for verifying webhook signature (x-payplus-signature = HMAC-SHA256(JSON))
PAYPLUS_SIGNING_SECRET=
